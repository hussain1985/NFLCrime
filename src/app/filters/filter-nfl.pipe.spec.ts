import { FilterNFLPipe } from './filter-nfl.pipe';

describe('FilterNFLPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNFLPipe();
    expect(pipe).toBeTruthy();
  });
});
